After completing a complex task, fixing a tricky error, or discovering a non-trivial workflow, save the verified approach as a reusable skill. When an agent-owned skill is outdated, incomplete, or wrong, patch it immediately.
